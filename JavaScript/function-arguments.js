// create a function that takes three arguments, and performa math operations on those arguments
function math (primary, secondary, last){
  var i = secondary * last;
  var result = i + primary;
  return result;
}

// print result of math function to console
console.log(math( 53, 61, 67 ));
