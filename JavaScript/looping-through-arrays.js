// loop through pets array until array length is reached and add an s to each array item
var pets = ['cat', 'dog', 'rat'];
  for (var i = 0; i < pets.length; i++) {
    pets[i] = pets[i] + 's';
}

// print result to console
console.log (pets);
