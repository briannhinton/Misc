// create variables
var total = 0;
var limit = 10;

// create variable i and loop i by 1 up to the definied limit variable, and adds number i to total variable
for ( var i=0; i < limit; i++ ) {
  total += i;
}

// print total to console
console.log(total);
